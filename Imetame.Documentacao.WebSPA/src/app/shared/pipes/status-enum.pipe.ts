// import { Pipe, PipeTransform } from '@angular/core';
// import { StatusEnum } from 'app/models/enums/Status.Enum';

// @Pipe({
//   name: 'statusPipe'
// })
// export class StatusEnumPipe implements PipeTransform {
//   transform(value: any, statusList: StatusEnum[]): string {
//     let status = statusList.find(s => s.valor == value);
//     return status ? status.nome : 'Desconhecido';
//   }
// }
